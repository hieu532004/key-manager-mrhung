// Keep tracked fallback accounts empty so no real credential is committed.
// Configure admin/support through ADMIN_USERS_JSON or other env vars in
// `.env.local` / Vercel Environment Variables.
export const ACCOUNTS = [];
